<Window x:Class="SatoshiTicker.AmountDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Bitcoin-Bestand eingeben"
        Width="420"
        Height="220"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#202020"
        Foreground="White"
        ShowInTaskbar="False">
  <Grid Margin="22">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <TextBlock Text="Dein aktueller Bitcoin-Bestand"
               FontSize="17"
               FontWeight="SemiBold"
               Margin="0,0,0,10"/>

    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>

      <TextBox x:Name="AmountTextBox"
               Height="36"
               FontSize="16"
               Padding="8,4"
               VerticalContentAlignment="Center"
               KeyDown="AmountTextBox_KeyDown"/>

      <TextBlock Grid.Column="1"
                 Text="BTC"
                 FontSize="16"
                 FontWeight="SemiBold"
                 VerticalAlignment="Center"
                 Margin="10,0,0,0"/>
    </Grid>

    <TextBlock Grid.Row="2"
               Text="Beispiel: 0,00128456 BTC — hier wird kein Eurobetrag eingetragen."
               Foreground="#B8B8B8"
               TextWrapping="Wrap"
               Margin="0,8,0,0"/>

    <StackPanel Grid.Row="3"
                Orientation="Horizontal"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom">
      <Button Content="Abbrechen"
              Width="96"
              Height="34"
              Margin="0,0,8,0"
              IsCancel="True"/>
      <Button Content="Speichern"
              Width="96"
              Height="34"
              IsDefault="True"
              Click="Save_Click"/>
    </StackPanel>
  </Grid>
</Window>
