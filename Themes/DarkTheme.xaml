<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <SolidColorBrush x:Key="TickerBackgroundBrush" Color="#F21B1B1B"/>
    <SolidColorBrush x:Key="TickerBorderBrush" Color="#555555"/>
    <SolidColorBrush x:Key="PrimaryTextBrush" Color="White"/>
    <SolidColorBrush x:Key="SecondaryTextBrush" Color="#B8B8B8"/>
    <SolidColorBrush x:Key="DialogBackgroundBrush" Color="#202020"/>
    <SolidColorBrush x:Key="PopupBackgroundBrush" Color="#F22A2A2A"/>
    <SolidColorBrush x:Key="PopupBorderBrush" Color="#555555"/>

    <DropShadowEffect x:Key="TickerShadow"
                      BlurRadius="0"
                      ShadowDepth="0"
                      Opacity="0"/>

    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="{DynamicResource PopupBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource PopupBorderBrush}"/>
    </Style>

    <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
    </Style>

    <Style TargetType="ToolTip">
        <Setter Property="Background" Value="{DynamicResource PopupBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource PopupBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>
</ResourceDictionary>
